#include <stdio.h>
#include <QFileInfo>
#include <QDir>
//#include "DnaIndex.h"


int main(int ai_ArgumentCount, char **ac_Arguments__) 
{
  /*
	if (ai_ArgumentCount < 2)
	{
		printf("Usage: gpfdump [--check] [GPF index file]\n");
		exit(1);
	}

	bool lb_Check = false;
	QString ls_Filename = ac_Arguments__[1];
	if (ls_Filename == "--check")
	{
		lb_Check = true;
		ls_Filename = ac_Arguments__[2];
	}
	k_DnaIndex lk_DnaIndex(ls_Filename);

	if (lb_Check)
		lk_DnaIndex.Check();
	else
		lk_DnaIndex.Dump();
  */
}
